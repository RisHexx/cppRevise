#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    std::ios_base::sync_with_stdio(false);
    // Null Pointer is pointer holding null values
    // while declaring pointer if we dont know where he is going to point assign it to null will be 
    // best practice as we dont want to it point randomly

    int *ptr = nullptr;
    cout<<ptr<<endl;
    cout<<*ptr<<endl;
    
    return 0;
}